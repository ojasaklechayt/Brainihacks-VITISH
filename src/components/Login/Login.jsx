// Import the React library
import React from "react";

// Define the Login component
const Login = () => {
    // Return a div
    return (
        <div>
            // Return a h1 with the text Login
            <h1>Login</h1>
        </div>
    );
};

// Export the Login component
export default Login;
